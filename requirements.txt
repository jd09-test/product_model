# OracleGraph — Python Dependencies
# Install with: pip install -r requirements.txt

# Oracle 23ai target database driver (thin mode, no Instant Client needed)
oracledb


# MCP server framework used by property_graph_mcp.py
fastmcp



